# Automaton Auditor - Environment Variables Template
#
# IMPORTANT: This file contains PLACEHOLDERS only, NOT actual API keys.
# Copy this file to .env and replace placeholders with your actual keys.
# Never commit your .env file to version control.

# OpenAI API Key (required for LLM models)
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API Key (optional, for vision models)
GOOGLE_API_KEY=your_google_api_key_here

# LangSmith Tracing (optional but recommended for observability)
LANGCHAIN_TRACING_V2=true
LANGCHAIN_API_KEY=your_langsmith_api_key_here
LANGCHAIN_PROJECT=automaton-auditor

# Per-layer model configuration
# Layer 1: Detectives (can use cheaper models)
LAYER1_MODEL=gpt-4o-mini
LAYER1_PROVIDER=openai

# Layer 2: Judges (need high-quality reasoning)
LAYER2_MODEL=gpt-4o
LAYER2_PROVIDER=openai

# Layer 3: Chief Justice (optional, uses Layer 2 by default)
LAYER3_MODEL=gpt-4o
LAYER3_PROVIDER=openai